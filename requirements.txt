# GeneralAI dependency entry point.
#
# Runtime, optional, and development dependencies are declared in
# pyproject.toml ([project.dependencies], [project.optional-dependencies]).
# This file installs the package in editable mode together with its
# development extras so `pip install -r requirements.txt` keeps working.
#
# Optional feature extras (not included by default):
#   pip install -e .[pdf]        # pypdf loader
#   pip install -e .[html]       # beautifulsoup4 HTML loader
#   pip install -e .[embeddings] # sentence-transformers embeddings
#   pip install -e .[chroma]     # chromadb vector store
#   pip install -e .[faiss]      # faiss vector store
-e .[dev]
